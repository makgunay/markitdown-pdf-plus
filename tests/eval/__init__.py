"""Real-document eval harness and quality metrics (TEDS + LLM-judge).

Not part of the shipped package and excluded from the unit-test collection
(`--ignore=tests/eval`); the metric modules here are imported by `tests/test_teds.py`
and `tests/test_judge.py`, which is why this is a package.
"""
